//! `pkdump-server` — the Axum HTTP application for PokeDumpster.
//!
//! Serves the JSON API under `/api` and the static SvelteKit build
//! (PLAN.md §5). Routes and middleware are added by later M1/M2 tasks.
