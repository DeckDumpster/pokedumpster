start_page: /browse/sv3pt5
involves:
  - "label > input[type=checkbox] bound to `includeSecret`, checked by default"
  - "onchange resetPage()"
  - "div.divider 'Secret Rares' inserted before the first slot whose section === 'secret'"
  - "the binder fetch passes secret: includeSecret to /api/binder"
fixture_data:
  set: "151 (sv3pt5), printed_total 165"
  secret_rare: "Mew ex #201, Hyper Rare — number_sortable 201 > printed_total 165, so it renders in the Secret Rares section"
notes: >
  1. Navigate to /browse/sv3pt5. 2. Page to the last page (or otherwise
  reveal the secret section) and assert the "Secret Rares" divider and the
  Mew ex slot (#201) are visible. 3. Uncheck the "Secret" checkbox. 4. Assert
  the page indicator resets to "Page 1 of N", the "Secret Rares" divider is
  gone, and no slot for #201 is rendered across the pages. 5. Re-check
  "Secret"; assert the Mew ex slot is reachable again. 6. Screenshot.
