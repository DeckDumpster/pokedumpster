# Scenario: Clicking a Pokémon's ability runs an ability: catalog search
#
# Related:
#   issues: [pokedumpster-cww]
#   pull_requests: []

description: >
  When I click a Pokémon's ability name on the card page, the browser
  navigates to the collection search filtered to that ability across the
  whole catalog — an `ability:` DSL query.
