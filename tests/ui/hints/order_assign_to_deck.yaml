start_page: /ingest/order
involves:
  - 'per-line Set / # inputs and "Look up" button'
  - 'per-line variant <select> populated with the card printings'
  - 'variant select defaults to the first printing'
  - '"Commit order" button'
fixture_data:
  resolve_set: "base1"
  resolve_number: "58"
  resolved_card: "Pikachu"
  variants: ["normal", "reverse_holo"]
notes: >
  Navigate to /ingest/order. Add a line, enter set base1 and number 58,
  click "Look up". Verify the line resolves to Pikachu and the variant
  select offers normal and reverse_holo, defaulted to the first. Select
  reverse_holo. Verify the line is still ready. Click "Commit order" and
  verify navigation to the order detail page.
