start_page: /collection
involves:
  - "'Select' button toggles multi-select mode"
  - "one table row per physical copy (strict one row per card)"
  - "per-row checkboxes for selecting a single copy"
  - "bulk 'Assign to deck…' <select> in the bulk-action bar"
  - "bulk assign loops one moveCopy call per selected copy, then refresh()"
fixture_data:
  search_term: "Charmander"
  card_name: "Charmander"
  card_set_number: "sv3pt5 #4"
  copies: 2
  target_deck: "Pikachu ex Aggro"
notes: >
  Navigate to /collection. Search for "Charmander" — two Charmander
  copies exist (from order TCG-100001), so two rows appear. Click
  "Select" to enter multi-select mode. Check exactly one Charmander row.
  In the bulk-action bar choose "Pikachu ex Aggro" from the "Assign to
  deck…" <select>. After the re-fetch only the selected copy has moved;
  the other Charmander copy is unchanged.
