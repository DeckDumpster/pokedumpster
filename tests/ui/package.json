{
  "name": "pkdump-ui-tests",
  "version": "0.1.0",
  "private": true,
  "description": "PokeDumpster intents UI testing framework — Claude Vision + Playwright.",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "yaml": "^2.6.0"
  }
}
