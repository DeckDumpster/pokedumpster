start_page: /ingest/order
involves:
  - 'paste textarea'
  - '"Parse" button'
  - 'lines section stays empty after a no-match parse'
fixture_data:
  garbage_text: "this is random text that is not an order format"
notes: >
  Navigate to /ingest/order. Fill the paste textarea with garbage text.
  Click "Parse". Verify no card lines were appended — the lines header
  still reads "Cards (0/0 ready)" and the empty state "No cards on this
  order yet." is still shown.
